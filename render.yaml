databases:
  - name: agentview-db
    plan: basic-1gb
    databaseName: agentview
    user: agentview
    region: virginia

services:
  # HTTP API Server
  - type: web
    name: agentview-api
    runtime: docker
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: .
    dockerCommand: pnpm run http:start
    region: virginia
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - fromGroup: AgentView 
      - key: DATABASE_URL
        fromDatabase:
          name: agentview-db
          property: connectionString
      - key: AGENTVIEW_API_PORT
        value: "10000"
      - key: BETTER_AUTH_SECRET
        generateValue: true

  # Background Worker
  - type: worker
    name: agentview-worker
    runtime: docker
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: .
    dockerCommand: pnpm run worker:start
    region: virginia
    plan: starter
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agentview-db
          property: connectionString
