databases:
  - name: agentview-db
    plan: starter
    databaseName: agentview
    user: agentview
    region: virginia

services:
  # HTTP API Server
  - type: web
    name: agentview-api
    runtime: docker
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: .
    region: virginia
    plan: starter
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agentview-db
          property: connectionString
      - key: AGENTVIEW_API_PORT
        value: "10000"
      - key: AGENTVIEW_STUDIO_URL
        sync: false
      - key: BETTER_AUTH_SECRET
        generateValue: true

  # Background Worker
  - type: worker
    name: agentview-worker
    runtime: docker
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: .
    dockerCommand: pnpm run start:worker
    region: virginia
    plan: starter
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agentview-db
          property: connectionString
